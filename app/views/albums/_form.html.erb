<%= form_for @album do |f| %>
  <tr>
    <td><%= f.text_field :title %></td>
    <td><%= f.text_field :date %></td>
    <td><%= f.text_field :cover_art %></td>
    <td><%= f.submit "Submit" %></td>
  </tr>
<% end %>
