<%= form_for @fan do |f| %>
  <tr>
    <td><%= f.text_field :name %></td>
    <td><%= f.text_field :email %></td>
    <td><%= f.text_field :password %></td>
    <td><%= f.submit "Submit" %></td>
  </tr>
<% end %>